<form method="post" action="{{ path('app_skill_category_delete', {'id': skill_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this category? This action cannot be undone.');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill_category.id) }}">
    <button class="btn btn-danger rounded-pill"><i class="fas fa-trash me-2"></i>Delete</button>
</form>
