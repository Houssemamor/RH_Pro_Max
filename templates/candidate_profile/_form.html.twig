{{ form_start(form, {'attr': {'class': 'row g-3'}}) }}
    <div class="col-12">
        {{ form_errors(form) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.fullName) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.email) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.status) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.createdAt) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.jobOffer) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.internalApplicant) }}
    </div>

    <div class="col-12 d-flex justify-content-end">
        <button class="btn btn-primary rounded-pill px-4">
            <i class="fas fa-save me-2"></i>{{ button_label|default('Save') }}
        </button>
    </div>
{{ form_end(form) }}
